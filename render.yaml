services:
  - type: web
    name: rbac-admin
    envVars:
      - key: DATABASE_URL
        value: file:/var/data/dev.db
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: "3000"
    disk:
      name: sqlite-data
      mountPath: /var/data
      sizeGB: 1
